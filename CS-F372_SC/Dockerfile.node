# Use Node image (version up to you)
FROM node:18-alpine

# Create a working directory inside the container
WORKDIR /app

# Copy only package.json/package-lock.json first
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy the rest of your code
COPY . .

# Expose the port (if your Node.js application runs on port 3000)
EXPOSE 3000 

# Start the server
CMD ["node", "server.js"]